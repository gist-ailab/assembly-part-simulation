file_name: "stefan_0.png"
assembly_region: [102, 405, 800, 302] # 조립 영역 bbox, [x_min, y_min, width, height]

group:
  - group_name: "ikea_stefan_side_left" # group_info를 불러오는 key로 사용
    mask: [[10,11,12,13,...]] # assembly_region의 좌상향 point를 원점으로 기준
    pose: # quaternion

  - group_name: "ikea_stefan_long"
    mask: [[13,15,12,13,...]] # assembly_region의 좌상향 point를 원점으로 기준
    pose: # quaternion 

  - group_name: "ikea_stefan_middle"
    mask: [[13,15,12,13,...]] # assembly_region의 좌상향 point를 원점으로 기준
    pose: # quaternion 

  - group_name: "ikea_stefan_short"
    mask: [[13,15,12,13,...]] # assembly_region의 좌상향 point를 원점으로 기준
    pose: # quaternion 

connector:
  - connector_name: "ikea_wood_pin(14ea)"
    bbox: [80, 100, 10, 10]
  - connector_name: "ikea_wood_pin(14ea)"
    bbox: [85, 95, 10, 10]
  - ...

connection:
  - connection_id: 0
    parts: # 서로 연결되는 파트  
      - "ikea_stefan_middle"
      - "ikea_wood_pin(14ea)" 
      - "ikea_stefan_side_left"
    bboxes : # 위치 정보
  - ...

hole:
  - hole_id: 1
    mask: [ ~ ]
  - hole_id: 2
    mask: [ ~ ]
