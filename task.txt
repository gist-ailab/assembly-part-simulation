조립 과정 세분화
1. 초기화 과정
    - 현재 설명서 단계 정보를 입력 받아 이 단계에서 조립해야할 부품(instance 단위) 구체화
    - 이전 조립 단계 마지막 status들을 불러 이번 단계 조립의 시작 상태로 지정

2. 조립 과정
    - 조립 단계에서 추출한 현재 단계에서 조립해야할 물체를 조립 순서에 맞게 정렬(해야할 일)
        [part1, part2, part3, part4...]
    - 현재 순서에 조립해야할 파트 두개를 서로 조립함.
    - 조립 결과물을 저장
        - status 파일로 저장
        - 조립 시에 parent가 누구였는지.(다음 조립 대상)
    - 조립 완료된 parent파트와 다음 파트 조립
    - 반복
3. 파트 간 조립 과정
    - 조립을 진행할 다큐먼트를 특정 이름으로 저장
        각 단계별로 수행하기 때문에, 그냥 index로 assemlby_index / status_index
        각 다큐먼트는 조립 수행할 파트를 불러올때 사용 되므로, 다큐먼트 내의 부모 파트가 다큐먼트의 주인
    - 부모 파트 다큐먼트, 추가될 파트 다큐먼트 로부터 파트 불러오기
        - 불러진 파트는 "다큐머트 이름_001" 라벨로 저장됨(obj.Label)
        - 불러진 파트들의 크기를 비교하여 부모 정하기(나중에 출력시 알려줘야 할듯)
        - 
        -> 각 인스턴스는 현재 어느 다큐먼트 인지를 status에 저장해야?
    - assemlby_points들의 현재 index를 얻어야함.(생각보다 여려울 듯)
        사용된 포인트와 사용 가능 포인트들 관리해야함.
        포인트를 불러올때, 위치 및 좌표

# commit
- furniture info의 point id 다시 추가
- 1 constraint working
- remove same object constraint



#TODO:
- 